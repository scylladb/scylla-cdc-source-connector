name: PR Verify

on:
  pull_request:
    branches:
      - master
  push:
    branches:
      - master

jobs:
  run-integration-tests:
    uses: ./.github/workflows/compatibility-test.yml
    secrets: inherit
